// Notifications as a persistent bottom tab. Re-exports the existing notifications
// screen so the home-header bell (which routes to /notifications) and the tab share
// one implementation.
export { default } from "../notifications";
